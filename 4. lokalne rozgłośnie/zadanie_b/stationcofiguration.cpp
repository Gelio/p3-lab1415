#include "stationconfiguration.h"

// w tym pliku nic nie zmieniamy

StationConfiguration::StationConfiguration(int stationCount, bool* interferringStations) : stationCount(stationCount), interferringStations(interferringStations)
{

}
