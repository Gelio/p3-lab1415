#include "stationconfiguration.h"

// w tym pliku nic nie zmieniamy

StationConfiguration::StationConfiguration(int stationCount, const std::vector<std::pair<int, int> > interferringStations) : stationCount(stationCount), interferringStations(interferringStations)
{

}
